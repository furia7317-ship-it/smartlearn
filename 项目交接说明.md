# 学枢项目交接与启动说明

> 最后核对：2026-08-19
> 项目版本：`0.1.14`
> 当前主分支：`main`
> 当前远端提交：`e4600d0da913cf7b6a0e493b1029bb12c32786f5`

## 1. 项目在哪里

| 项目 | 位置 |
|---|---|
| Windows 源码目录（实际开发目录） | `D:\816` |
| WSL 访问路径 | `/mnt/d/816` |
| Windows 交接入口 | `D:\816\项目交接说明.md` |
| GitHub 私有仓库 | `https://github.com/furia7317-ship-it/smartlearn` |

重要说明：源码已经迁回 `D:\816`。WSL 中不再保留 `/home/furia/projects/816` 副本；从 WSL 开发、运行测试和执行 Git 操作时统一使用 `/mnt/d/816`。

从 Windows Terminal 进入项目：

```powershell
wsl -d Ubuntu
cd /mnt/d/816
```

## 2. 项目是什么

学枢是一个面向高等教育的 AI 个性化学习桌面应用：

- 前端：Next.js 16、React 19、Electron，桌面页面入口为 `/desktop/`。
- 后端：FastAPI、LangGraph、SQLAlchemy、ChromaDB。
- 核心能力：多智能体资料生成、RAG 知识库、学习画像、学习路径、练习与错题、课件与视频等多模态资源。
- 数据方式：账号、SQLite 数据库、模型配置、密钥、向量库和媒体产物均为本机运行时数据，不在 Git 仓库中。

主要目录：

```text
/mnt/d/816/
├── frontend/    # Next.js 页面、组件、Electron 桌面壳
├── backend/     # FastAPI、LangGraph、数据库与服务代码
├── knowledge/   # 内置课程知识库 Markdown 源文件
└── README.md    # 项目总体介绍
```

## 3. 当前机器上的正常启动方式

当前环境已经安装 Node、前端依赖和后端虚拟环境。开发时需要打开两个 WSL 终端，先启动后端，再启动前端。

### 当前运行架构（必须先看）

- Codex/开发终端运行在 Windows PowerShell 7。
- 源码真实存放在 Windows 的 `D:\816`。
- 当前 `backend/.venv` 与 `frontend/node_modules` 是 Linux 依赖，因此前后端进程仍由 WSL Ubuntu 启动。
- Windows 内置浏览器通过 `http://127.0.0.1:3000` 访问 WSL 中的服务。
- “源码迁回 Windows”不等于“项目已经改成 Windows 原生运行”。若要彻底移除 WSL 依赖，必须重新建立 Windows Python 虚拟环境并重新安装 Windows 版 npm 依赖，不能直接复用现有目录。

截至 2026-08-19 交接时，后端 `8000` 与前端 `3000` 已由 WSL systemd 临时服务托管，内置浏览器已打开 `/desktop/` 并恢复本机账号。服务不会依赖某一个终端窗口存活，但执行 `wsl --shutdown` 或重启 Windows 后需要重新启动。查看状态和日志：

```bash
sudo systemctl status smartlearn-backend smartlearn-frontend
sudo journalctl -u smartlearn-backend -u smartlearn-frontend -f
```

由于 WSL 在没有 Windows 客户端连接时会自动关闭，当前 Windows 任务计划程序中还有一个正在运行、但未来触发器已禁用的 `Smartlearn WSL Keepalive` 任务，用于维持本次交接会话。它不会在重启后自动拉起；日常开发仍建议使用下方两个前台终端启动。

停止当前托管服务：

```bash
sudo systemctl stop smartlearn-backend smartlearn-frontend
```

随后可在 PowerShell 中结束并移除本次保活任务：

```powershell
Stop-ScheduledTask -TaskName 'Smartlearn WSL Keepalive'
Unregister-ScheduledTask -TaskName 'Smartlearn WSL Keepalive' -Confirm:$false
```

### 终端一：启动后端

```bash
cd /mnt/d/816/backend
source .venv/bin/activate
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

看到 Uvicorn 启动完成后，可访问：

```text
http://127.0.0.1:8000/
```

### 终端二：启动前端

```bash
cd /mnt/d/816/frontend
npm run dev
```

浏览器打开：

```text
http://127.0.0.1:3000/desktop/
```

新机器首次使用需要在登录页自行注册账号，仓库不会预置或生成共享测试账号。当前机器的本地数据库中已有用于验收的 `furia` 账号与学习痕迹；账号和密码不会进入 Git 仓库。

停止服务时，在两个终端中分别按 `Ctrl+C`。

## 4. 新机器或环境被重建后的初始化

推荐版本：

- WSL：Ubuntu
- Node.js：24（仓库 `.node-version` 已固定为 `24`）
- Python：3.10 或更高版本
- 当前验证环境：Node `v24.19.0`、npm `11.17.0`、Python `3.10.12`

### 初始化后端

```bash
cd /mnt/d/816/backend
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
test -f .env || cp .env.example .env
```

随后编辑 `backend/.env`，填入实际使用的模型或媒体服务配置。不要把 `.env`、密钥或账号数据提交到 Git。

### 初始化前端

```bash
cd /mnt/d/816/frontend
npm ci
```

完成后按照第 3 节启动。

## 5. 配置和本地数据

- 配置模板：`backend/.env.example`
- 本机配置：`backend/.env`，已被 Git 忽略
- 默认数据库：`backend/smartlearn.db`，已被 Git 忽略
- 向量库、媒体文件、构建结果和日志同样不进入仓库
- 至少配置一个可用的 OpenAI 兼容模型服务，真实生成与答疑能力才能工作
- 模型服务也可以在应用的“设置 → 模型与服务”中配置
- 新环境第一次加载嵌入模型或知识库可能较慢，应等待后端完成初始化

交接时只传源码和 `.env.example`。实际密钥应通过安全渠道单独交接，不能写入本文档或 GitHub。

## 6. 启动后的快速验收

```bash
curl http://127.0.0.1:8000/
curl -I http://127.0.0.1:3000/desktop/
```

然后在桌面页面确认：

1. 能注册并登录新账号。
2. 页面右上角或设置页显示后端已连接。
3. 首页分页和页面过场动画正常。
4. AI 工坊能够发起真实请求；未配置模型时应显示明确错误，而不是生成占位内容。
5. 刷新后本机学习数据仍能恢复。

## 7. 测试与构建

前端：

```bash
cd /mnt/d/816/frontend
npm test
npx tsc --noEmit
npx eslint app components hooks lib electron
npm run build
```

后端：

```bash
cd /mnt/d/816/backend
source .venv/bin/activate
python -m pytest
```

构建 Windows 桌面安装包：

```bash
cd /mnt/d/816/frontend
npm run app:dist
```

安装包输出到 `frontend/dist-electron/`。该命令会先准备自包含 Python、知识库和视频运行时，耗时明显长于普通前端构建。

## 8. 常见问题

### 页面提示后端未连接

先确认 `http://127.0.0.1:8000/` 能访问，并检查启动后端的终端输出。前端可以独立打开，但生成、检索和账号等真实业务需要后端。

### 3000 或 8000 端口被占用

```bash
ss -ltnp | rg ':3000|:8000'
```

关闭占用端口的旧进程后重新启动，不要同时运行多套后端并共用同一个 SQLite 文件。

### GitHub 无法拉取或推送

仓库是私有仓库，接手人需要先获得 GitHub 访问权限并使用自己的账号登录。当前机器的 GitHub CLI 已登录 `furia7317-ship-it`，但凭据属于本机用户配置；更换机器或 Windows 用户后需要重新授权，不要把令牌写入仓库。

### 前端能打开但模型调用失败

检查 `backend/.env` 或应用设置中的模型 Base URL、API Key 和模型名。不要通过修改前端代码硬编码密钥。

## 9. 本轮已完成内容

本轮主提交为 `e4600d0`（`feat: complete desktop learning workspace overhaul`），已经推送到 GitHub `main`。主要完成内容如下：

1. 重做桌面端整体视觉体系，统一为克制的书院案头风格；更新左侧导航、学枢牌匾、页面过场和响应式布局，并移除不匹配的铜环装饰。
2. 重做“发现”模块，加入卷轴山水主视觉、互动课堂、精选课程与学习市场入口；“开始探索”会进入学习市场。
3. 深度重做“资源中心”：加入真实书封、开合书与翻页动画、左右书页布局、生成视频和外链资源能力，并将 RAG 知识库从普通用户资源中拆出独立入口。
4. 资源中心收起状态只保留“进入知识库”和“AI 生成资料”两个主入口；展开状态支持资料预览、批量选择、集合管理、发布学习路径和媒体操作。
5. 新增真实的资源集合后端、前端数据层与测试；用户可以创建集合并将已有资料放入集合。
6. 将智能教师入口改为小气泡/浮动卡片形态，支持拖动，并修正退出登录后的状态与位置恢复问题。
7. 统一桌面模块的视图状态恢复，资源中心、学习路径等模块切换后会回到离开时的稳定状态。
8. 修复账户服务不可用、认证恢复与退出竞态问题；增加账号使用痕迹填充脚本 `backend/scripts/seed_account_usage.py`。
9. 补齐资源、认证、集合、媒体和桌面交互测试；一次性视觉验收记录与审计截图不再纳入源码仓库。
10. 将完整项目从 `/home/furia/projects/816` 迁移到 `D:\816`，验证零文件差异后删除 WSL 旧副本；远端和本地提交均已核对一致。

## 10. 验证结果与已知事项

### 已验证

- 前端生产构建通过，共生成 43/43 个静态页面。
- 前端测试：390 项，389 通过、0 失败、1 跳过。
- 迁移后前端测试再次通过，证明 `D:\816` 中的 Linux Node 依赖可在 WSL 正常使用。
- 后端应用可正常导入并初始化，应用名为“学枢”，共加载 36 条路由。
- 当前健康检查：`http://127.0.0.1:8000/` 与 `http://127.0.0.1:3000/desktop/` 均返回 HTTP 200。
- 内置浏览器已实际进入桌面首页，本机账号 `furia` 的学习痕迹、导航和资源入口可见，浏览器控制台未发现错误。

### 接力时重点注意

1. 后端全量测试曾出现 `630 passed, 1 failed`：失败用例为 `tests/test_resource_plan_api.py::test_inflight_cancel_stops_followup_calls_and_keeps_cancelled_terminal`。该用例单独运行可通过，表现为全套并发测试中的共享 SQLAlchemy session/执行顺序问题；不要把它误判为迁移损坏，但后续应继续消除测试隔离风险。
2. 源码位于 NTFS，首次从 WSL 启动会比 Linux 原生目录慢。本次后端首次初始化约一分钟，Next.js 首次编译也会额外整理文件系统缓存；应等待日志出现 `Application startup complete` 和 `Ready`。
3. WSL 中的深度学习用户包（PyTorch/CUDA/Triton）已删除；uv、pip、npm 缓存和 apt 索引也已清理。下次安装依赖会重新下载，安装 apt 包前先运行 `sudo apt update`。
4. WSL 虚拟磁盘曾从约 24.84 GB 压缩到约 5.92 GB；为运行本项目，Ubuntu 已重新启动。这不表示旧项目副本恢复，唯一源码仍是 `D:\816`。
5. 与本项目无关的 `/home/furia/reasonix/node_modules` 已清理，如需运行 reasonix，需在其目录重新安装依赖。
6. `.env`、SQLite、Chroma、用户账号、模型配置和生成媒体均为本机数据，不在 GitHub。不要把它们加入提交，也不要在交接文档中写入密钥。
7. 当前工作树可能存在未跟踪的 `.codex_tmp/` 运行日志和 `design-audit/*` 视觉资料。它们属于已忽略的本机运行/审计产物，不应提交；清理前仍应先判断用途，不要直接使用 `git clean` 或重置命令。
8. 当前账号数据已填充用于视觉和交互验收。若要测试首次注册/空账号流程，请创建独立账号，不要直接清空现有 SQLite 数据库。
9. 后端启动时会提示项目虚拟环境中的 PyTorch CUDA 构建与当前 NVIDIA 驱动版本不匹配。应用目前会继续启动并完成知识索引验证，普通 CPU 路径可用；如要使用 GPU 推理，应更新 Windows NVIDIA 驱动或在项目虚拟环境中安装与驱动匹配的 PyTorch 构建。

## 11. 交接检查清单

- [ ] 接手人能够进入 `D:\816`（WSL 下为 `/mnt/d/816`）
- [ ] 已单独、安全地取得所需服务凭据
- [ ] 后端 `8000` 和前端 `3000` 均能启动
- [ ] 能注册新的本机账号并登录
- [ ] 至少完成一次真实模型调用
- [ ] 前端测试、类型检查和生产构建通过
- [ ] 明确本地数据库和运行时产物不在 GitHub 中
